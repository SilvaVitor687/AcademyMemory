//const veiculos = require('../config/BD');


const indexControllers = {
    index: (req,res) => {       
       return (
        res.render('index')
    )},

    
}

module.exports = indexControllers;
